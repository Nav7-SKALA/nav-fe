server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    # 요청된 파일이 없으면 index.html로 포워딩 (SPA 라우팅용)
    try_files $uri $uri/ /index.html;
  }
}